const express = require("express") //importing express frameworks 
const app  = express()             // creating server instance 
app.listen(port, () =>{            // connecting to the port 
    console.log("Connected to Server") //displays this message 
})
